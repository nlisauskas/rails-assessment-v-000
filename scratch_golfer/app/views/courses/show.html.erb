<h1>Welcome to <%= @course.name %>.</h1>

<%= render 'info' %>

<%= link_to "Take a look at all of the rounds that have been played at #{@course.name}.", course_rounds_path %>

<%= link_to "Played here? Add a round.", new_round_path %>
